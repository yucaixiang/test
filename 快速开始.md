# CI/CD Pipeline - 5分钟快速开始

## 前置条件确认

确保以下服务已经运行:

```bash
# 检查Jenkins
curl -I http://localhost:30080

# 检查Kubernetes
kubectl get nodes

# 检查Docker
docker ps
```

---

## 快速部署步骤

### 1️⃣ 本地测试应用 (可选)

```bash
# 进入项目目录
cd /Users/bjsttlp324/Desktop/learn/software/CICD/demo-app

# 使用Maven编译和运行
mvn clean package
java -jar target/demo-app.jar

# 测试API (在另一个终端)
curl http://localhost:8080/api/hello
```

按 `Ctrl+C` 停止应用。

---

### 2️⃣ 配置Jenkins

#### 访问Jenkins
```bash
# 打开Jenkins
open http://localhost:30080

# 登录凭证
用户名: admin
密码: Admin@Jenkins2024
```

#### 安装Maven工具
1. **Manage Jenkins** → **Global Tool Configuration**
2. 找到 **Maven** → 点击 **Add Maven**
3. 配置:
   - Name: `Maven-3.9`
   - ✅ Install automatically
   - Version: `3.9.x`
4. 点击 **Save**

#### 创建Pipeline任务
1. 点击 **New Item**
2. 输入名称: `demo-app-pipeline`
3. 选择 **Pipeline**
4. 配置Pipeline:
   - **Pipeline script from SCM**
   - SCM: **Git**
   - Repository URL: `file:///Users/bjsttlp324/Desktop/learn/software/CICD/demo-app`
   - Branch: `*/main`
   - Script Path: `Jenkinsfile`
5. 点击 **Save**

---

### 3️⃣ 执行Pipeline

1. 在任务页面点击 **Build Now**
2. 查看 **Console Output** 观察构建进度

Pipeline包含6个阶段:
```
✅ 代码检出 → ✅ Maven构建 → ✅ 单元测试
     ↓
✅ Docker镜像构建 → ✅ 部署到K8s → ✅ 部署验证
```

预计耗时: **5-10分钟** (首次构建需要下载依赖)

---

### 4️⃣ 验证部署

#### 查看Kubernetes资源
```bash
# 查看Pod状态
kubectl get pods -l app=demo-app

# 期望输出:
# NAME                        READY   STATUS    RESTARTS   AGE
# demo-app-xxxxxxxxxx-xxxxx   1/1     Running   0          2m
# demo-app-xxxxxxxxxx-xxxxx   1/1     Running   0          2m

# 查看Service
kubectl get svc demo-app

# 期望输出:
# NAME       TYPE       CLUSTER-IP     EXTERNAL-IP   PORT(S)          AGE
# demo-app   NodePort   10.96.xxx.xxx  <none>        8080:30088/TCP   2m
```

#### 测试API端点
```bash
# 测试健康检查
curl http://localhost:30088/api/hello

# 期望输出:
# {
#   "status": "success",
#   "message": "Hello from Demo App!",
#   "timestamp": "2025-11-10 10:30:00",
#   "version": "1.0.0",
#   "environment": "Kubernetes"
# }

# 测试个性化问候
curl http://localhost:30088/api/greet?name=Jenkins

# 测试系统信息
curl http://localhost:30088/api/info

# 使用浏览器访问
open http://localhost:30088/api/hello
```

---

## 常用操作

### 查看应用日志
```bash
# 实时查看日志
kubectl logs -l app=demo-app -f

# 查看最近100行
kubectl logs -l app=demo-app --tail=100
```

### 重新部署
```bash
# 方式1: 在Jenkins点击 "Build Now"

# 方式2: 修改代码后重新构建
cd /Users/bjsttlp324/Desktop/learn/software/CICD/demo-app
# 修改代码...
git add .
git commit -m "Update feature"
# 然后在Jenkins触发构建
```

### 扩缩容
```bash
# 扩容到3个副本
kubectl scale deployment demo-app --replicas=3

# 查看状态
kubectl get pods -l app=demo-app -w
```

### 删除应用
```bash
# 删除所有资源
kubectl delete -f /Users/bjsttlp324/Desktop/learn/software/CICD/demo-app/k8s/

# 或使用标签删除
kubectl delete deployment,service -l app=demo-app
```

---

## 故障排查

### Pipeline失败

**问题: Maven构建失败**
```bash
# 检查Maven工具配置
# Jenkins → Manage Jenkins → Global Tool Configuration → Maven
# 确保Name为 "Maven-3.9" 且已勾选自动安装
```

**问题: Docker镜像构建失败**
```bash
# 检查Docker是否运行
docker ps

# 重启Docker Desktop
```

**问题: Kubernetes部署失败**
```bash
# 查看Pod详细信息
kubectl describe pod -l app=demo-app

# 查看事件
kubectl get events --sort-by='.lastTimestamp' | tail -20
```

### Pod无法启动

```bash
# 查看Pod日志
kubectl logs -l app=demo-app

# 常见错误:
# - ImagePullBackOff: 镜像不存在或拉取失败
#   解决: 检查Jenkins的Docker构建阶段是否成功
#
# - CrashLoopBackOff: 应用启动失败
#   解决: 查看Pod日志排查应用错误
#
# - Pending: 资源不足
#   解决: 检查节点资源 kubectl describe node
```

### API无法访问

```bash
# 1. 检查Service
kubectl get svc demo-app

# 2. 测试Pod内部访问
kubectl exec -it <pod-name> -- wget -O- http://localhost:8080/api/hello

# 3. 端口转发测试
kubectl port-forward svc/demo-app 8080:8080

# 在另一个终端测试
curl http://localhost:8080/api/hello
```

---

## 访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| **应用API** | http://localhost:30088/api/hello | Demo应用主接口 |
| **Jenkins** | http://localhost:30080 | CI/CD平台 |
| **Grafana** | http://localhost:30030 | 监控仪表板 |
| **Prometheus** | http://localhost:30090 | 指标收集 |
| **Rancher** | https://localhost:8443 | K8s管理平台 |

---

## 下一步

1. **查看详细文档**: `CICD/README.md`
2. **学习Pipeline配置**: 查看 `Jenkinsfile` 了解各阶段
3. **尝试修改代码**: 修改 `HelloController.java` 并重新部署
4. **配置监控**: 在Grafana中创建Dashboard监控应用
5. **学习高级功能**:
   - 多环境部署 (dev/test/prod)
   - 蓝绿部署
   - 金丝雀发布

---

## 相关文档

- **完整文档**: `/Users/bjsttlp324/Desktop/learn/software/CICD/README.md`
- **项目结构**: `/Users/bjsttlp324/Desktop/learn/software/CICD/demo-app/`
- **环境凭证**: `/Users/bjsttlp324/Desktop/learn/software/credentials-实际部署.txt`
- **学习路径**: `/Users/bjsttlp324/Desktop/learn/software/learning-path.md`

---

**恭喜你完成第一个CI/CD流程！** 🎉

继续探索更多功能，祝你学习愉快！ 🚀
